import Routes from "./routes";
import './App.css'

function App() {
  return <Routes />
}

export default App

/* para instalar la paginación, numeros de páginas, usaremos 
    npm install react-paginate --save

  para manejo de fechas instalaremos date-fns
  npm install --save date-fns

  manejador de eventos Zustand para no tener que realizar muchos llamados a las API cada vez que un componente requiere la data... 
  npm install --save zustand
 */
